name: mobile ci
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  availability:
    runs-on: ubuntu-24.04
    steps:
      - name: Require implemented capability
        run: |
          echo 'mobile ci is not implemented. See docs/development/progress.md.' >&2
          exit 1
